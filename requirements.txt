# AIMMS Migration Tool - Python Dependencies

# Core dependencies (built-in Python modules)
# Note: Most dependencies are built-in Python modules
# sqlite3 - Built-in SQLite3 module
# os - Built-in operating system interface
# shutil - Built-in high-level file operations
# logging - Built-in logging facility
# argparse - Built-in command-line argument parsing
# datetime - Built-in date/time handling
# uuid - Built-in UUID generation
# pathlib - Built-in object-oriented filesystem paths
# typing - Built-in type hints support


# Installation note:
# This project primarily uses Python built-in libraries.
# No external dependencies are required for basic functionality.
# Optional dependencies are listed for development, testing, and documentation.